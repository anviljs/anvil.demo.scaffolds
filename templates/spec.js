require( "should" );
var {{{module-name}}} = require("../src/{{{fileName module-name}}}");

describe( "Define a specification", function() {
	it( "should do something awesome", function() {
		true.should.be.ok;
	} );
} );